doctype html
html(lang="ja")
  head
    meta(charset="utf-8")
    title SQLインジェクションを試してみよう
  body
    h1 SQLインジェクションを試してみよう
    //a(href="/logout") ログアウト
    h2 名前作成
    form(method="post" action="/posts")
      div
        textarea(name="content" cols=40 rows=4)
      div
        button(type="submit" name="button" value="toko") 投稿
        span #{time}
    //form(method="post" action="/posts")
      //input(name="content" type="txt" size="30" maxlength="20")
      //div
        //br
        //button(type="submit" name="button" value="search") 検索
        span
    h2 投稿一覧
    each content in contents
      p 名前
      p!= content.name
      p!= content.email
      hr